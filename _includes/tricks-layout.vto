---
layout: base.vto
---

<div class="trick">
  <h1>{{ title }}</h1>
  <div class="mb-2">
    {{ for tag of tags.filter((tag) => tag !== "tricks") }}
      {{ await comp.TagChip({ tag }) }}
    {{ /for }}
  </div>
  {{ content }}
</div>
